/*#include <string>

class PriorityQueue{
public:
	static PriorityQueue* Instance();
	string getValue();
	void changeValue(string newVal);
private:
	PriorityQueue(){}; // Constructor private so it can't be called
	PriorityQueue(PriorityQueue const&){}; 
	static PriorityQueue* myQueue; // operater
	string value;
}*/